# Commande /self-index

Indexe le projet lolve-cartography lui-même.

## Instructions

```bash
node bin/cli.cjs --project . annotate index
```

Affiche ensuite les statistiques :
```bash
node bin/cli.cjs --project . annotate stats
```
